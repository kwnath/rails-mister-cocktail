<div class="container">
  <div class="row text-center">
    <div class="cocktails">
      <h1>Cocktails</h1>
      <% @cocktails.each do |c| %>
        <p> <%= link_to c.name, cocktail_path(c) %> </p>
      <% end %>
    </div>
  </div>
</div>
