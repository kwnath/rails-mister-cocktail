<div class="container">
  <div class="row text-center">
      <h1>Cocktails</h1>
      <% @cocktails.each do |c| %>
        <p> <%= link_to c.name, cocktail_path(c), class: 'no-textd' %> </p>
      <% end %>
    </div>
  </div>
</div>
